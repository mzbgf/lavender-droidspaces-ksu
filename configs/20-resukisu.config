# ============================================================
# [诊断分支 diag/noksu-console] —— 定位「fastboot boot 卡一屏、只有背光」
#
# 本分支与 main 的唯一区别：
#   1) 显式关闭 KSU：reSukiSU 内核驱动完全不编译（头号嫌疑：非 GKI manual hook）
#   2) 打开屏上 framebuffer console：让早期内核输出直接显示在屏幕上
#      （配合 cmdline 里的 console=tty0）
#
# 注意: 注释里不要写出符号名的完整前缀形式（参见 00-rom-align.config 顶部说明），
#       否则合并时会删掉基座 defconfig 里的同名项。
# ============================================================

# 显式关闭（该选项在上游 Kconfig 里 default y，必须写出关闭行才真的关掉）
# CONFIG_KSU is not set

# --- 诊断用：屏上 console ---
CONFIG_VT=y
CONFIG_VT_CONSOLE=y
CONFIG_DUMMY_CONSOLE=y
CONFIG_FRAMEBUFFER_CONSOLE=y
CONFIG_FONT_8x16=y
CONFIG_LOGO=y
