# ============================================================
# 00-rom-align.config — 与「目标 ROM 的 stock 内核 config」对齐
#
# 默认留空。基线（pix106/android_kernel_xiaomi_sdm660_southwest-ng）就是这些
# A16 ROM 实际使用的内核源码线，版本串已经一致。
#
# 版本串（uname -r）由三部分拼成：
#   4.19.325
#   + 树根 localversion-cip（内容 -cip135）
#   + 树根 localversion-st（内容 -st19）
#   + 基座 defconfig 里的 LOCALVERSION（-SouthWest-NG-0.19.4）
# 本配方的默认结果：4.19.325-cip135-st19-SouthWest-NG-0.19.4
#
# 本树基座 defconfig 里 MODULES 是关闭的（驱动全部内建），所以正常情况 ROM 侧
# 没有需要匹配 vermagic 的 .ko，版本串只影响 uname -r 的显示；但若你的 ROM 带
# 内核对侧模块、或者你换了别的基础树，就必须与 ROM 逐字一致。
#
# 需要改版本串时按优先级：
#   1) 首选：把 .github/workflows/build.yml 顶部的 KERNEL_PIN 换成对应 0.x.x
#      分支的 commit（该仓库 0.4.2 … 0.19.4 每个版本一个分支）；
#   2) 或者触发 workflow_dispatch 时填 localversion 输入项，构建脚本会生成
#      99-localversion.config 追加覆盖。
#
# ============================================================
# 【重要约束 · 必须遵守】
# 本文件是喂给 scripts/kconfig/merge_config.sh 的。它的行为是：从片段里解析出
# 符号名列表，然后对每个符号执行「删掉基座 defconfig 里同名的那一行」，再把片段
# 整段追加进去。也就是说——**只要片段里出现了带完整前缀的符号名（哪怕是注释、
# 哪怕是被 # 注释掉的赋值行），基座里对应的设置就会被删掉**，而如果片段里没有
# 同名真配置行补回来，该设置就会悄悄退回 Kconfig 默认值（LOCALVERSION 就是这么
# 被清空的）。
#
# 因此本仓库所有 configs/*.config 遵守两条规则，scripts/merge-configs.sh 会硬校验：
#   1) 注释里只写符号名本身，前缀形式请写占位符，例如 CONFIG_<符号>；
#   2) 合法的真配置行只有两种：
#        CONFIG_<符号>=y            （赋值行）
#        # CONFIG_<符号> is not set （显式关闭行）
#
# 若确实需要在这里覆盖某项，请把真正的配置行写出来（不带 # 前缀即可生效）。
# 常见需要对齐的项：LOCALVERSION、MODVERSIONS、MODULE_SIG、RD_LZ4、PSI。
# ============================================================
