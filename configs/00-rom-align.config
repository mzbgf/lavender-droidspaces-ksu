# ============================================================
# 00-rom-align.config — 与「目标 ROM 的 stock 内核 config」对齐
#
# 为什么需要：模块 vermagic 由内核版本串（CONFIG_LOCALVERSION + 树根
# localversion-* 文件）决定。只要我们的版本串与 ROM 现有内核一致，
# ROM 里现成的模块就还能加载；不一致就会出现 wlan/bt 起不来之类的问题。
#
# 默认**为空**：因为本仓库的基线（pix106/android_kernel_xiaomi_sdm660_southwest-ng）
# 就是这些 A16 ROM 实际使用的内核源码线，LOCALVERSION 默认
# "-SouthWest-NG-0.19.4" 与 ROM 一致，不需要额外对齐。
#
# 什么时候需要填：如果你的 ROM 内核版本串不是 0.19.4（例如 0.19.3 或更早），
# 有两条路：
#   1) 推荐：把 .github/workflows/build.yml 里的 KERNEL_PIN 换成对应
#      0.x.x 分支的 commit（仓库里 0.4.2 ... 0.19.4 每个版本一个分支）；
#   2) 或者在本文件里覆盖版本串，并在 workflow 里用 LOCALVERSION_OVERRIDE
#      传值（scripts/merge-configs.sh 会生成 99-localversion.config 追加）。
#
# 确认方法（在手机上）：
#   设置 -> 关于手机 -> 内核版本        # 或者
#   adb shell uname -r
# 与我们构建产出的 kernelrelease 比对（CI 日志里会打印）。
#
# 下面这些是需要重点比对的项（按需取消注释）：
# CONFIG_LOCALVERSION="-SouthWest-NG-0.19.4"
# CONFIG_MODVERSIONS=y
# CONFIG_MODULE_SIG=n
