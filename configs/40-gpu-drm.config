# DRM：只为了提供 /dev/dri/renderD128 这一个渲染节点
#
# 背景：本机显示走 CAF fbdev（FB_MSM），GPU 走 KGSL，原生内核没有 DRM 子系统。
# Wayland 的正确解法是 anland（渲染走 kgsl、上屏交给 Android Surface），不经过 KMS，
# 因此这里并不需要 DRM 的显示能力。
#
# 唯一用途：anland 的 Chroot/LXC 启动路径写死了
#     ANLAND_DRM_DEVICE=/dev/dri/renderD128
# 该 fd 只作 freedreno 的 control_fd（fd 身份 / 屏幕缓存键），GPU 命令仍走
# /dev/kgsl-3d0。Droidspaces 属于这类容器，所以留一个渲染节点备用。
#
# 若最终采用 anland 的 PRoot 路径（ANLAND_NO_DRM_DEVICE=1 +
# EGL_PLATFORM=surfaceless），则这两个开关可以整体删除。
#
# 设计约束：不动 KGSL、不动 FB_MSM、不动设备树，Android 侧零影响。
# 依赖项（DRM、KMS_HELPER、GEM 等）由 olddefconfig 自动补齐，这里只钉住两个开关。
# 提醒：本文件的注释里不能写带前缀的符号名，否则 merge_config 会删掉基座的同名行。
CONFIG_DRM=y
CONFIG_DRM_VKMS=y
